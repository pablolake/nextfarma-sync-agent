# Conexión SQL Server (Docker local)
DB_SERVER=localhost
DB_PORT=1433
DB_NAME=Farmatic
DB_CONSEJO=Consejo
DB_USER=sa
DB_PASSWORD=NextFarma2026!
DB_ENCRYPT=false
DB_TRUST_CERT=true
DB_WINDOWS_AUTH=false

# Rellenar con credenciales reales
API_KEY=fxi_7fd77a1c0b1aa964d77d5797d14cb5a8db9451dd13940027cc183953ba752665
TENANT_ID=5fef7a1d-1f95-4b96-add2-e851c9a099e5
API_BASE_URL=https://api-production-3d66.up.railway.app


# Configuración sync
LOG_LEVEL=debug
BATCH_SIZE=500
EXCLUDED_VENDORS=99

# Labs SC (IDs de proveedor en Farmatic — confirmar con wizard)
LAB_CINFA=E0111
LAB_NORMON=E0426
LAB_KERN=E0863
LAB_TEVA=E1079
SC_UMBRAL=2500
SC_CINFA_NORMON=0.05
SC_KERN_TEVA=0.10

# Listas de categoría/color deliberadamente SIN configurar — replica el caso Jose-2 (ninguna
# de las 7 listas de categoría existe todavía) para probar que asegurarListas() las crea
# solas contra farmatic-test.sql (que ahora solo trae una lista "FAVORITOS", ver ese archivo).
# Si una prueba anterior ya las creó en este mismo contenedor, hay que reiniciar el volumen
# (docker compose down -v) para volver a probar la creación desde cero.
